message: "Welcome to API Gateway!"
spring:
  cloud:
    gateway:
      routes:
        - id: ProductService
          uri: lb://product-service
          predicates:
            - Path=/product/**
        - id: OrderService
          uri: lb://order-service
          predicates:
            - Path=/order/**
